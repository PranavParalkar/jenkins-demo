# Admin secret for privileged endpoints
admin.secret=Pranav273!
spring.application.name=gamesta

spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:root}
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:db}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:gamesta}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC


# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Server port (optional)
server.port=8080

# Razorpay keys (set these before testing)
razorpay.key=rzp_test_Rha05G68H41uVK
razorpay.secret=3rPBLZfTH8GapGNm66W0Dl8j

# Socket.io configuration
socketio.host=localhost
socketio.port=9092

# SMTP mail settings (configure via environment variables)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gamestamitaoe@gmail.com
spring.mail.password=srqrlxiqbkrkmkjs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.from=${GAMAIL_FROM:${spring.mail.username}}

